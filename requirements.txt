# TP Banco de Dados — dependências Python
# (versões testadas)

pandas>=2.0
numpy>=1.24
matplotlib>=3.7

# Renderização de diagramas
graphviz>=0.20      # python wrapper (precisa do binário 'dot' instalado no SO)

# Geração de relatório e slides
python-docx>=1.0
python-pptx>=1.0

# Notebook
jupyter>=1.0
ipykernel>=6.0

# PostgreSQL (modo de produção)
psycopg2-binary>=2.9
sqlalchemy>=2.0
